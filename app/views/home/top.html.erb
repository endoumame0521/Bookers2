<div class="top-message offset-3 col-md-6">

  <h1>
  	welcome to 
    <span class="fas fa-book"><strong>Bookers</strong>  !!</span>
  </h1>

  <p>In  <span class="fas fa-book"></span><strong>Bookers</strong>, </p>

  <p>you can share and exchange  your opinions , inpressions , and emotions</p>
  <p> about various books!</p>


      <% if user_signed_in? %>
      <%= link_to 'Log in', user_path(current_user.id), class: "btn btn-info col-12 offset-1" %>
       <% else %>
      <%= link_to 'Log in', new_user_session_path, class: "btn btn-info col-12 offset-1" %>
       <% end %>


       <% if user_signed_in? %>
      <%= link_to 'Sign Up', user_path(current_user.id), class: "btn btn-success col-12 offset-1" %>
       <% else %>
      <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-success col-12 offset-1" %>
      <% end %>


</div>